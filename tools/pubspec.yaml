name: tools
description: A project for inspecting device characteristics.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  device_info: ^2.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter 

flutter:
  uses-material-design: true